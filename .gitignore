# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.env
.venv

# ETL generated files
data/processed/
data/failed/
data/archive/
logs/
reports/
*.log
etl_errors.log

# Test files
test_csv.py
*.csv
# Remove the gitignored files from Git (but keep them locally)
git rm --cached -r data/processed/ data/failed/ data/archive/ logs/ reports/

# For individual files
git rm --cached logs/etl_errors.log logs/etl_pipeline.log
git rm --cached crm-project/logs/etl_errors.log 2>/dev/null || true

# For CSV files in the root (if they should be ignored)
git rm --cached .csv 2>/dev/null || true
